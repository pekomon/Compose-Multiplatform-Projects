package com.example.pekomon.minesweeper

import kotlin.test.Test
import kotlin.test.assertTrue

/**
 * Very small smoke test to ensure the test pipeline is wired.
 * Extend with real domain logic tests as features land.
 */
class AppSmokeTest {
    @Test
    fun runs() {
        assertTrue(true)
    }
}
